import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },

  backgroundMsg: {
    width: 350,
    backgroundColor: "rgba(120,5,5,0.95)",
    paddingTop: 20,
    paddingBottom: 20,
    marginTop: 20,
    marginBottom: 30,
  },

  failMsg: {
    color: "white",
    fontSize: 32,
    textAlign: "center",
  },
});
